"""14. Closures as Stateful Functions ← we are here

Closures that remember changing state
nonlocal
Encapsulated state
Closures vs classes
Stateful functions and state machines

15. Closures vs Classes

When a closure is the better abstraction
When a class is clearly better
Encapsulation
Multiple behaviors and complex state
Practical backend examples

Then we move on to:

Module 6 — Iterators & Generators as Functional Tools

16. Lazy Functional Pipelines

Why laziness matters
map()/filter() revisited at a deeper level
Generators as lazy transformations
Memory efficiency
Streaming large datasets
Combining generators with functional pipelines

17. itertools

chain
islice
takewhile / dropwhile
accumulate
groupby
product
permutations
combinations
cycle / repeat

And this module is particularly relevant to your backend goals because we'll connect it to processing large datasets, streaming, ETL pipelines, and memory-efficient services.

So for now, let's finish stateful closures, then closures vs classes, and we'll enter generators/iterators."""